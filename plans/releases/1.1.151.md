# 1.1.151 — cached full-readback landing의 중복 host memcpy 제거

2026-09-20. 기준 1.1.150 HEAD 7da51221. 메인 checkout과 빌드는 F:/melonDS, build/windows-dev다.

## 구현 — 조건부 copy 생략

full-readback 체인은 합성 이미지를 renderer 소유 host-visible `readback` buffer에 내린 뒤 무조건 `hostReadback` vector로 memcpy했다(16x ~2.0ms, 48MiB). 런타임 probe가 이 buffer의 실제 memory flags가 0x0e(HOST_VISIBLE|HOST_COHERENT|**HOST_CACHED**)임을 확인했다 — cached landing에서는 copy가 중복 cached→cached pass다.

`ComputePipeline::FullReadbackPixels()`가 cached이면 mapped `readback` span을 직접 반환하고, uncached fallback이면 기존 memcpy+vector를 유지한다. RenderView·ReadbackView 둘 다 사용. fullReadbackValid·consume-before-reuse 계약·native readback·fallback 불변.

## 검증

Pro run 20260920T102955Z-fdd75d0375(approved run-root, model latest/pro)이 구현·측정·초점 검증을 완료했으나 최종 보고서 작성 전에 정지했다 — 부모가 적용 diff가 candidate.patch와 동일함을 확인하고 최종 초점 검증을 대신 실행했다(local-docs/pro-vk-fullcopy-20260920/result.md).

정확성: capture 벤치 행렬 30행이 baseline과 display·native·sidecar digest·submits·waits·traffic 전부 동일. 초점 56/56이 baseline·candidate OFF/ON에서 통과했고 부모가 적용 상태에서 OFF 56/56(67.60s)·ON 56/56(65.89s)으로 재확인했다. FullCopyBytes 계약은 실제 memory flag에 귀속하는 정확한 조건부로 갱신됐다(cached=0·uncached=FullReadbackBytes) — assertion 약화가 아니라 새 계약의 정확한 반영이다.

측정(같은 checkout·장치, diagnostics ON 평균): 16x에서 full_memcpy **2.05~2.14ms → 제거**, RunFrame wl2 14.81→12.77·wl3 15.68→13.49·wl5 126.26→123.43·wl7 54.46→52.37ms. 8x에서도 0.52~0.54ms 제거. B-only(4·6)는 이 경로를 안 쓴다. wait_full_rb(~0.92ms) 불변.

부모 검증: 전체 빌드 exit 0. 최종 소스 전체 CTest **995/995** 통과(local-docs/pro-vk-fullcopy-20260920/full-ctest.log). git diff --check exit 0.

## 남은 범위·한계

- 측정값은 생성 fixture의 RunFrame 시간이다 — 실게임 FPS·Qt 표시 지연이 아니다.
- uncached landing 장치는 memcpy를 유지한다 — 그 경로는 이 장치에서 미측정이다.
- 실기·DSi·다른 OS/GPU·대화형 UI·장기 실행은 미검증이다.

## 배포 상태

(패키징 후 기록)