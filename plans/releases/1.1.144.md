# 1.1.144 — 타입이 있는 표시 프레임 계약(GR-16)

2026-09-20. 기준 1.1.143 HEAD 92c9a87f. 메인 checkout과 빌드는 F:/melonDS, build/windows-dev다.

## 구현 — DisplayFrame view

지금까지 표시 질의는 bool+void** 한 쌍이었다: true는 RAM BGRA, false는 renderer 특정 값(GL texture array id)을 뜻하고 수명·완료·반납 규칙은 암묵적이었다. 두 번째 GPU API(Vulkan)가 연결된 지금, GR-16의 최소 단위로 타입이 있는 Renderer::DisplayFrame view가 GetDisplayFramebuffers를 대체한다. kind(CpuBGRA/GLTexture2DArray)·표시 크기·payload·generation을 명시하고, GetDisplayFrame은 프레임 이용 가능 여부만 반환한다. native guest/capture 소비자는 GetFramebuffers를 그대로 쓴다.

계약(주석으로 강제): view는 생산자의 다음 프레임 swap·SetRenderSettings·renderer 교체까지 유효하고, 소비자는 renderLock 아래에서만 역참조·매번 재조회한다(1.1.134 규칙의 계약화). generation은 NDS.NumFrames에서 출발하는 host 전용 단조 stamp로, swap·설정/크기 변경·교체·guest NumFrames rewind에서도 이전 view를 부활시키지 않는다. CpuBGRA는 반환 시 완료, GLTexture2DArray는 생산자 프레임 종료 제출 후 완료(기존 공유 context 순서). 생산자가 버퍼/texture를 소유하고 다음 swap에 재사용하며 반납 신호는 없다. 미래 GPU-image kind는 acquire fence+release 신호 의무를 예약만 한다.

소비자 이관: ScreenPanelNative는 CpuBGRA만 받아들이고 paint에서 재조회한 지역 view로 복사한다(Screen.h의 게시 포인터 멤버 제거). ScreenPanelGL은 kind로 CPU 업로드/texture 바인드를 명시 분기하고, 프레임 부재 시 화면 본문을 건너뛰되 기존 present/OSD 흐름은 유지한다. EmuInstance::preserveFrame도 같은 kind 분기다. 생산자: 기본 구현은 GetFramebuffers를 CpuBGRA 256x192로 감싸 SoftRenderer는 무수정, GLRenderer는 texture id+실제 배율 크기, VulkanRenderer는 CPU 합성 ScaledBuffers를 돌려준다.

설계 결정과 직접 표시 보류 근거(GPU에 최종 프레임 부재·Presenter/renderer의 별개 VkDevice·효용 미측정)는 local-docs/pro-gr16-frame-20260920/design.md에 있다.

## 검증

Pro run 20260919T191544Z-8b7bb59d5943(approved run-root, model_check verified)이 구현·초점 검증을 완료했다. 첫 시도 run 20260919T191135Z-e34b3b445ab4는 DevSpace 읽기의 일시 Connection failed로 미실행 종료(durable capture)됐고, funnel 의 401 확인 후 새 run으로 대체했다.

GR-16 gate: 추출된 실제 ScreenPanelGL::drawScreen이 실제 NDS 생산자를 backend별로 생성→present→재사용→일시정지 resize→교체하는 5-present 수명 검증을 통과했다(gl-presentation-frame-lifetime-{software,opengl,compute,vulkan}). generation은 재조회에 안정적이고 swap·설정·교체·rewind에서 단조 증가하며, 새 native-display-frame-generation fixture가 payload 역참조 없이 24개 검사를 통과했다. GL texture readback은 presentation 이후에만 일어나 생산자 쪽 대기를 추가하지 않는다.

초점 회귀: GL/native/presentation 77/77 + Vulkan renderer 19/19 = 96/96 통과(focused-gl-native.log, focused-vulkan.log, lifecycle-verified.log). git diff --check exit 0. 부모 전체 빌드·전체 suite·패키징은 이 문서의 배포 상태 절을 따른다.

## 남은 범위

실제 게임 표시·실기·DSi·다른 OS/GPU·대화형 UI·장기 실행은 미검증이다. VkImage 표시·GPU 2D 합성·장치 공유·external-memory interop·acquire/release 구현은 이 단위에 없다. ROMSmoke는 빌드만 됐고 실행은 ROM이 필요해 미실행이다.

## 배포 상태

deploy-windows.py와 package-windows.py는 모두 exit 0으로 완료했다. build/1.1.144-packages의 runtime ZIP은 103,904,230바이트·325파일, source ZIP은 18,290,019바이트다. Source ZIP 기준은 eb4e1b48715ba6b098155533969bf370111cc538이며, EXE source identity·버전(1.1.144), manifest 해시, ZIP 무결성, 깨끗한 PATH의 --help(exit 0)를 패키지 도구에서 확인했다. 알려진 개인 파일 포함 수는 0이다. 기존 license supplement(local-docs/2026-09-14/release-1.1.117/license-supplement.json)를 재사용했다. 번역 catalog와 D3D12 dxcompiler/dxil 부재 경고는 유지한다. 이 패키지 확인은 D3D12·실게임·설치 GUI 수락 증거가 아니다.

Runtime ZIP SHA-256: aff6893ae5c56c0f8b158e3c8f0a24f1135ddb96de961ac89eb047c1ff0ced58.
Source ZIP SHA-256: b97a3ddeb893c2ab1bf5ff898f8990c4990c85506691a6f273ac92a4e94f7ef7.
EXE SHA-256: 20d5cd7099f5343c83f80bcf85c8863b0506003027d09f230770673ed9f4e7d1.
