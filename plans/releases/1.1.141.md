# 1.1.141 — 모자이크 스프라이트 행의 캡처 bitmap OBJ 디테일

2026-09-20. 기준 1.1.140 HEAD e09ebf3c. 메인 checkout과 빌드는 F:/melonDS, build/windows-dev다.

## 구현 — 모자이크 행의 캡처 OBJ 디테일

이전에는 적격 모자이크 스프라이트가 있는 행에서 CaptureOBJScale·CaptureOBJActive를 지워 그 행 전체의 캡처 향상을 껐다. 이제 그 행을 표시 전용 모자이크 처리로 표시(CaptureOBJMosaic)하고 캡처 버퍼를 유지한다. DrawSpritePixel은 각 native X의 실제 승자(투명 래치 메타데이터 교체 포함)에 대한 캡처 provenance를 CaptureOBJProvenance[256]에 기록하며, 진 captured 스프라이트가 native-only 승자를 captured로 표시하지 못한다. ApplySpriteMosaicX는 기존 4-규칙 native 래치 결정으로 소스 X를 고르고, 각 목적지는 소스의 scale 폭 블록을 모든 캡처 subrow에 받는다. 래치된 승자에 캡처 provenance가 없으면 그 목적지 블록만 post-latch native OBJ 값으로 채운다(행 전체 fallback이 아님). CaptureOBJActive는 실제 소비된 래치 소스의 유효 provenance로 재계산된다. Y OBJMosaicLine 조정·top underflow clamp·OBJ-window 제외·X width 1의 native 항등은 그대로다.

## 검증

Pro run 20260919T163043Z-749ad20db8f3(approved run-root, model_check verified)이 구현·초점 검증을 완료했고 부모가 최종 diff를 한 번 검토했다. git diff --check exit 0.

Test-first: 수정된 fixture의 16 모자이크 케이스+94 고정 좌표 probe가 원래 소스에서 3x/5x 양 engine 모두 실패(디스플레이 서브픽셀 오류 234/228/584/569)했고, 같은 fixture가 구현 후 6/6 설정을 통과했다. provenance guard를 captured=true로 바꾼 의도적 mutation은 3x engine-0에서 정확히 18개 디스플레이 오류로 검출됐다(복구 후 최종 blob 동일).

최종 초점 CTest 14/14 통과 25.54초(final-focused.log): 6 OBJ 설정 × 19 케이스·103 고정 probe·7210 서브픽셀 비교 오류 0, 기존 5x XY-flip boundary 양 engine 통과, 96/96 공통 케이스 full-native-frame FNV-1a 해시 불변, 캡처된 guest VRAM 131,072바이트 불변. 부모 전체 빌드 exit 0. 최종 소스 전체 CTest는 **978/978, 실패·skip 0, 284.72초**다(release-ctest.log).

## 남은 범위

실제 게임 모자이크 동작·실기 대조·DSi·다른 OS/GPU 조합·대화형 UI·장기 실행·성능 영향은 미검증이다. 모자이크 테스트는 affine identity와 Y 반전을 다루며 모든 affine/double-size 행렬과의 조합은 아니다. allocation-failure 주입과 savestate 전용 테스트는 추가하지 않았다.

## 배포 상태

deploy-windows.py와 package-windows.py는 모두 exit 0으로 완료했다. build/1.1.141-packages의 runtime ZIP은 103,893,333바이트·325파일, source ZIP은 18,268,471바이트다. Source ZIP 기준은 ea6bfc58b206734514622ed858c59f0ce9016919이며, EXE source identity·버전(1.1.141), manifest 해시, ZIP 무결성, 깨끗한 PATH의 --help(exit 0)를 패키지 도구에서 확인했다. 알려진 개인 파일 포함 수는 0이다. 기존 license supplement(local-docs/2026-09-14/release-1.1.117/license-supplement.json)를 재사용했다. 번역 catalog와 D3D12 dxcompiler/dxil 부재 경고는 유지한다. 이 패키지 확인은 D3D12·실게임·설치 GUI 수락 증거가 아니다.

Runtime ZIP SHA-256: b8ad9f281ebf46b17b8398d5ecd7beb7c39b4f9691702ef001f8b5da3819306e.
Source ZIP SHA-256: 37c80f7defb680d634f2d53145f6938a9bc463d78be4ea8a918e02e87f6ec611.
EXE SHA-256: 2dc1e211fb0c05f636556e9a4baa5756a2f5a9246c6a20f9821ac840f22a8ff6.
