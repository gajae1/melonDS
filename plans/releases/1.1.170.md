# 1.1.170 — ARM7 JIT 다중 레지스터 전송 타이밍 보정

2026-09-24. 이전 버전 1.1.169. 기본 배포 설정은 SDL2이며 SDL3는 `ENABLE_SDL3=ON` 선택형 빌드다.

ARM7 x64 JIT가 메인 RAM 스택으로 컴파일한 `LDM` 블록을 나중에 ARM7 WRAM 스택에서 재사용하면, 읽은 레지스터 값은 현재 스택을 따르지만 사이클은 이전 스택 비용을 유지했다. 같은 게스트 코드와 진입 시각의 게임 추적에서 캐시 블록은 24사이클, 새 추적은 13사이클이었고, 차이 11사이클은 다중 전송 하나에서 발생했다. 이번 변경은 실행 전 실제 전송 주소별 비용과 첫 주소의 메인 RAM 분류를 확인한다. 컴파일 당시와 다르면 부작용 전에 해당 명령 하나를 기존 해석기에서 실행한다. ARM9의 기존 다중 전송 검사 경로는 유지된다.

집중 회귀 검사는 여섯 레지스터 `LDMIA sp!`를 메인 RAM 스택에서 컴파일한 뒤 WRAM 스택으로 캐시 재사용하고, WRAM에서 새로 컴파일한 블록 및 해석기와 레지스터·SP·PC·CPSR·사이클을 비교한다. 수정 전 코어는 값과 상태가 같아도 사이클 `19/8/8`로 실패했고, 수정 후 집중 검사는 통과했다.

같은 비공개 게임 입력의 프레임 6001 기준/저장 후 JIT 리셋 비교에서는 두 경로의 화면 해시가 같고, 이전에 달랐던 ARM7 WRAM 바이트 `0x0380FF20`와 Timer5 재장전값이 일치했다. 그러나 타이머·ARM9를 포함한 저장 상태 차이는 남아 있어 **BV-17은 미해결**이다. 이 결과는 확인된 다중 전송 오류의 보정 범위이며, 전체 상태 동일성 또는 실제 기기 타이밍을 주장하지 않는다. 비공개 ROM, savestate와 원시 계측 파일은 배포하지 않는다.

프레임 12000까지의 비공개 기준/리셋 장기 비교에서도 사전 상태와 이후 **6000개 화면 해시 및 최종 PPM이 각각 일치**했다. 그러나 최종 저장 상태에는 MainRAM·ARM7 WRAM·Timer5·ARM9·GPU3D의 실질적 차이가 남는다. 양 화면의 일치는 상태 동일성의 대체 근거가 아니다. 이 장기 화면 해시열과 최종 PPM은 기존 1.1.167 기준 화면 결과와도 일치했지만, 비교 가능한 1.1.169 장기 저장 상태는 없었다.

소스 커밋 `484c9ce83239870edb0ce5b6f9dd07162441c651`의 깨끗한 복제본에서 Windows UCRT64 Release를 기본 SDL2와 선택형 SDL3로 각각 빌드했다. SDL2 전체 CTest는 **1023/1023**(새 다중 전송 회귀 검사 포함), SDL3 오디오·입력 집중 CTest는 **48/48** 통과했다. 두 패키지의 소스 식별자는 `a480d9cb4b7f2cfd077bdba596e2ffdde30495832bcfe316a67578040a1cbd65`로 같다. 다른 작업자의 기존 teakra 수정은 깨끗한 복제본과 패키지에 들어가지 않았다.

두 배포와 패키징은 각각 exit 0이다. 제한된 시스템 PATH의 `--help` 검사, manifest 및 ZIP 검증을 통과했다. 각 런타임 ZIP은 325개 파일, 알려진 비공개 파일 0개를 담고, 기본 ZIP에는 SDL2 DLL만, 선택형 ZIP에는 SDL3 DLL만 있다.

| 산출물 | SHA-256 |
| --- | --- |
| `build/1.1.170-packages-sdl2/1.1.170-melonDS-windows-x64.zip` (기본) | `33d1b6bbc6ccef8667768b65d34a6bc2fd573095056c7c4bd446b6dfeb2274ef` |
| `build/1.1.170-packages-sdl3/1.1.170-melonDS-windows-x64.zip` (SDL3 선택형) | `36eb6edd6c86d53864b0070c6ffada04d4a63ddbd8e4fbb8137c84507be1c49c` |
| 양쪽 패키지의 동일한 `1.1.170-melonDS-source.zip` | `59ae89d94317b5f90b95de50cc584ae0a63890b509fc6d6d884fee9ae90a0c4a` |

두 런타임 ZIP을 각기 새 폴더에 풀고, 한글 이름의 게임 archive와 새 프로필로 첫 실행했다. SDL2와 SDL3 모두 `[60/60]` 창에서 실제 게임 타이틀/메뉴 양 화면(320×508)을 육안 확인했고, `VCDJ` 카트 진입·부팅 로그, `WM_CLOSE` exit 0, 실행 파일·archive·원본 ROM·기존 작업 파일 해시 보존을 확인했다. 비공개 캡처 SHA-256은 SDL2 `f307dfb459e1186f40d0204d274123b6f4150a107dc0aaf9ed625a083151a929`, SDL3 `372f5d1f605e3b642ec42fbe67c61235c9e145c2052a31b4f1bf7a04461925b3`이다. 원시 기록은 로컬 `local-docs/release-170-20260924/`에 있다. 한 메뉴 화면은 실제 조작·저장 영속성·장기 진행·물리 스피커/마이크/패드 수락을 뜻하지 않는다.

패키지 해시와 첫 실행 기록은 패키징 후 추가한 문서이므로 위 소스 ZIP의 릴리스 문서에는 최종 표가 없다. 실행 코드와 패키지 소스 식별자는 바뀌지 않았다.
