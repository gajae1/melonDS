# 렌더러 완성 계획을 위한 코드 감사

기준: 1.1.02 소스 분석. 아래는 재현·측정·구현할 후보와 검증 계약이다. 정적 의심을 확인된 결함으로 읽지 않는다. 원래 관찰은 보존하며 현재 처리 상태는 [과제 목록](../Task_Catalog.md), 실제 변경은 [릴리스 기록](../releases/1.1.03.md)을 따른다.

| ID | 현상·구체 트리거 / 파일:함수 | 검증 수준 | 최소 구현 단위 | 선행 작업 | 최소 의미 있는 검증·장치 gate | 효용 / 위험 / 우선순위 |
|---|---|---|---|---|---|---|
| <a id="GR-01"></a>GR-01 | 캡처가 GPU에만 남은 채 같은 backend에서 배율 변경: [GPU_OpenGL.cpp](../../src/GPU_OpenGL.cpp) GLRenderer::SetScaleFactor가 CaptureOutput128/256을 nullptr로 재할당. [EmuThread.cpp](../../src/frontend/qt_sdl/EmuThread.cpp) updateRenderer의 동일 backend 경로는 GPU::SetRenderer의 사전 동기화를 거치지 않음 | 정적 의심: 이전 캡처 RAM/texture 참조 유실 가능 | 재할당 전 캡처 보존과 capture metadata 전환을 한 설정 경계로 처리 | pending/synced block 각각의 소유권·설정 적용 시점 확정 | source A/B 캡처→1x↔2x→CPU 읽기·후속 texture 재사용 비교; 128/256 및 offset wrap, 실제 캡처 게임 | guest 데이터 보존 / 잘못된 drain·무효화는 캡처 손상 / P1 |
| <a id="GR-02"></a>GR-02 | 같은 capture texture의 연속 variant에서 Sampler만 변경: [GPU3D_Compute.cpp](../../src/GPU3D_Compute.cpp) ComputeRenderer3D::RenderFrame은 texunit을 매번 0으로 시작하고 Texture 변경 때만 1/2로 설정하여 새 sampler를 unit 0에 묶을 수 있음 | 정적 의심: 조건 분기는 확인, 픽셀 재현 없음 | texture 종류로 대상 unit을 매 variant에서 결정; 기존 shader/texture 묶음 유지 | wrap/mirror가 다른 두 capture polygon 입력 구성 | 128/256 capture의 clamp/repeat/mirror 전환, 일반 texture로 왕복; 실제 GPU 픽셀과 바인딩 경로 모두 비교 | 특정 캡처 texture 왜곡 방지 / batching 상태 회귀 / P1 |
| <a id="GR-03"></a>GR-03 | GL 3.2 fallback context에서도 Compute 선택 경로가 남고, [GPU3D_Compute.cpp](../../src/GPU3D_Compute.cpp) ShaderCompileStep은 CompileShader 결과를 버린 채 33까지 증가. [Screen.cpp](../../src/frontend/qt_sdl/Screen.cpp) createContext, [VideoSettingsDialog.cpp](../../src/frontend/qt_sdl/VideoSettingsDialog.cpp) 생성자 | 정적 의심: 미지원/컴파일 실패가 완료로 보일 경로 | 실제 capability로 선택 제한; 첫 compile 실패를 renderer 실패 상태로 전달하고 기존 software fallback과 UI 상태 일치 | 필요한 GL 기능 집합·실패 시 마지막 유효 renderer 소유권 확정 | 3.2-only context 및 한 shader 실패 주입; 무효 dispatch 없이 복구·재선택. 캐시 hit/miss 모두 확인 | 시작 실패·검은 화면 진단 / fallback 중 자원·설정 불일치 / P1 |
| <a id="GR-04"></a>GR-04 | 반복 classic GL 생성/파괴: [GPU3D_OpenGL.cpp](../../src/GPU3D_OpenGL.cpp) GLRenderer3D::~GLRenderer3D에 Init의 IndexBufferID 및 clear/final-pass 네 program 해제가 보이지 않음. 초기 shader 실패 시 [GPU_OpenGL.h](../../src/GPU_OpenGL.h) GLRenderer의 미초기화 GLuint, [GPU3D_OpenGL.h](../../src/GPU3D_OpenGL.h) ClearBitmapTex도 정리 대상 | 정적 의심: 생성/삭제 비대칭·초기값 공백 확인, 실제 생존 객체 수 미측정 | 누락된 객체 종류의 해제와 실패 전 ID=0 보장; 대형 RAII 전환은 불필요 | 생성·공유·소유 객체를 구분, GR-03 실패 경로 | 기존 fixture recorder를 buffer/program/texture에 필요한 만큼 확장; context 유지 teardown·첫 shader 실패에서 다른 객체 생존 보존 | 반복 전환·실패 복구 / 공유 객체 오삭제 / P1 |
| <a id="GR-05"></a>GR-05 | 캡처 진행 중 CPU 읽기·CaptureCnt 변경: [GPU_OpenGL.cpp](../../src/GPU_OpenGL.cpp) GLRenderer::SyncVRAMCapture는 complete=false를 로그 후 처리; [GPU.cpp](../../src/GPU.cpp) CheckCaptureEnd는 변경된 레지스터로 완료 block을 판단하는 TODO가 있음 | 정적 의심: 어느 scanline까지 읽혀야 하는지 실행/실기 미검증 | 먼저 한 mid-capture 입력의 시점·block 완료 상태를 재현; 필요한 부분만 latch/부분 동기화 | guest CPU/DMA 접근 시각과 DS 기대 동작 확보; GR-06 fixture | 캡처 도중 읽기/쓰기, 목적지·크기 변경, 중복 읽기의 값·dirty 상태를 SW/GL 및 실기로 대조 | guest RAM 정확성 / 타이밍 변경 위험 큼 / P1 |
| <a id="GR-06"></a>GR-06 | source B 합성 통과만으로 source A 3D→capture→guest CPU까지 닫을 수 없음: [GLFrameReadback.cpp](../../tests/GLFrameReadback.cpp) main, CheckComputeSampling; [GPU_OpenGL.cpp](../../src/GPU_OpenGL.cpp) DoCapture, DownscaleCapture | 관찰: 기존 실행 범위의 공백, 새 결함 확정 아님 | 기존 fixture에 실제 source A와 A+B 한 시나리오부터 추가 | 올바른 15-bit 색·alpha·크기·bank wrap 기대값; 기존 barrier 유지 | 두 GL backend·SW, 1x/확대, CPU/DMA 소비; NVIDIA 후 AMD/Intel, 실제 readback 게임 | 잘못된 완료 판정 방지 / fixture가 producer 앞에 동기화를 추가하면 결함 은폐 / P1 |
| <a id="GR-07"></a>GR-07 | 큰 배율에서 tile/SSBO/texture 재할당 실패: [GPU3D_Compute.cpp](../../src/GPU3D_Compute.cpp) SetRenderSettings가 기존 shader/texture 및 scale 상태를 먼저 바꾸고 여러 glBufferData를 호출 | 정적 의심: 한도·실패 복구 경로 공백, OOM 재현 없음 | device 한도와 할당 결과로 지원 배율 산정; 실패 시 기존 유효 출력 또는 software로 복구 | GR-01·GR-03·GR-04 | 제한 한도/할당 실패 주입→배율 선택·재시도; 작은 VRAM GPU에서 고배율 검증. 증가량 측정 전 메모리 절약 수치 금지 | 저사양 호환성 / 재시도·부분 할당 수명 / P2 |
| <a id="GR-08"></a>GR-08 | equal-depth wireframe, translucent clear polygon ID, screen-edge/alpha-only fog 조합: [GPU3D_Soft.cpp](../../src/GPU3D_Soft.cpp) RenderPixel, ScanlineFinalPass; [GPU3D_OpenGL.cpp](../../src/GPU3D_OpenGL.cpp) RenderSceneChunk, RenderFrame; [GPU3D_Compute.cpp](../../src/GPU3D_Compute.cpp) RenderFrame | 정적 의심: approximation/TODO와 비교 공백, 게임 결함 확정 아님 | 차이가 나는 특성 하나씩 입력·DS 기대값·최소 수정으로 분리 | 공통 입력 replay와 기준 픽셀/깊이/attribute; SW를 무조건 정답으로 삼지 않음 | Z/W, shadow, fog/AA/edge, clear bitmap, texture format·palette·wrap·dirty 조합을 최소 직교 사례로; 실기와 특성 사용 게임 | 렌더러 간 정확도 / 근사 수정의 다른 게임 회귀 / P1 |
| <a id="GR-09"></a>GR-09 | [GPU2D_Soft.cpp](../../src/GPU2D_Soft.cpp) ColorComposite, DrawScanline_BGOBJ와 [GPU3D_Soft.cpp](../../src/GPU3D_Soft.cpp) ScanlineFinalPass의 연속 정수 처리 | 미측정 아이디어: C1/C2 SIMD·동일 효과 구간 묶음 | profile에서 큰 한 커널만 scalar 동등 최적화; ISA별 자체 framework 금지 | GR-08 기대값·GR-14 측정, 기존 dispatcher·scalar SWAR 재사용 | bias·clamp·flag·u32 overflow·tail/unaligned, 전체 frame 비교; Intel/AMD 실측과 NEON 실제 ARM. 느리면 채택하지 않음 | CPU 여유 가능 / 정수 정밀도·packing 비용 / P2 |
| <a id="GR-10"></a>GR-10 | 다양한 크기 texture가 반복 교체될 때 [GPU3D_Texcache.h](../../src/GPU3D_Texcache.h) GetTexture/Update는 layer를 재사용하나 array 전체 해제는 Reset에 집중; MaskedHash 비용·상주량 미계측 | 미측정 아이디어: high-water 유지이지 확인된 누수 아님 | cache hit/hash/decode/upload/array 상주량 계측 후, 필요할 때만 유휴 array 회수 한 정책 | 사용 중 layer 참조·cached variant 수명 파악 | palette/VRAM bank remap·같은 주소 재업로드·크기 순환, cache on/reset 결과 비교; 장기 게임 메모리·tail 측정 | 메모리/업로드 비용 가능 / 유효 texture 조기 회수·thrash / P2 |
| <a id="GR-11"></a>GR-11 | surface 재생성·실패 시 [Screen.cpp](../../src/frontend/qt_sdl/Screen.cpp) initOpenGL/drawScreen은 MakeCurrent·compile·SwapBuffers 성공을 상위 상태로 충분히 전달하지 않음; RAM/GL 출력과 shared context 경계 | 정적 의심: 실패 주입·실제 화면 재현 없음 | 현재화/표시 실패를 한 상태 경로로 전달하고 해당 surface 재생성 또는 기존 native 표시로 복구 | renderer/context 소유 thread·파괴 순서, GR-03 | resize·최소화·휴면 복귀·혼합 DPI·회전·다중 창 재개, touch 좌표와 게임 프레임 보존; WGL 및 지원할 각 OS 장치 | 화면 복구·입력 일치 / 락·교차 context 수명 / P2 |
| <a id="GR-12"></a>GR-12 | [graphics/gl/context.cpp](../../src/frontend/graphics/gl/context.cpp) GL::Context::Create의 static context_being_created와 global glad loader는 Not thread-safe 주석. 복수 인스턴스가 동시에 생성할 때 잘못된 context 조회 후보 | 정적 의심: 동시 호출 도달성 자체 미입증 | 호출 thread부터 확인; 가능할 때만 loader 초기화 구간 직렬화/소유권 정리 | 실제 frontend 생성 호출 순서, GR-11 | 서로 다른 context의 동시 생성/종료를 제한 fixture로 재현; 항상 직렬이면 후보 종결. Windows와 추가 지원 OS | multi-instance 안정성 / 과도한 global lock·driver별 차이 / P2 |
| <a id="GR-13"></a>GR-13 | render 진행 중 threaded 전환·pause/save/load/중단: [GPU3D_Soft.cpp](../../src/GPU3D_Soft.cpp) SetThreaded, RenderThreadFunc, FinishRendering, GetLine; abort 시 cleared line 반환에 정확도 TODO | 정적 의심: 현재 deadlock/화면 오류 재현 없음 | scanline 공개·중단·join 계약을 기존 흐름에 기록하고 실패한 전환 한 건만 수정 | core pause/savestate 담당의 호출 경계 | 같은 상태에서 threaded on/off 결과·pause/load 반복·종료 무대기; 실제 ARM에서도 publication 확인 | 정지/재개 일관성 / 동기화 변경 위험 / P2 |
| <a id="GR-14"></a>GR-14 | [측정 계약](../Validation.md), [GPU_OpenGL.cpp](../../src/GPU_OpenGL.cpp) DrawScanline/DoCapture/SyncVRAMCapture, [Screen.cpp](../../src/frontend/qt_sdl/Screen.cpp) drawScreen 사이 실제 비용 분해 부족 | 미측정 아이디어: 안전한 병목의 위치 아직 불명 | 공통 workload에 CPU render·upload·GPU 처리·readback·present 대기를 구분한 제한 계측 | 동일 장면·입력·배율·limiter·driver, 계측 overhead 확인 | median·p95와 stall 빈도·전송량; 기존 fixture의 glFinish 포함 수치를 게임 비동기 성능으로 해석하지 않음 | 최적화 선택 근거 / 계측이 scheduling을 바꿈 / P2 |
| <a id="GR-15"></a>GR-15 | Vulkan 제품 backend 부재: [GPU.h](../../src/GPU.h) Renderer, [EmuThread.cpp](../../src/frontend/qt_sdl/EmuThread.cpp) updateRenderer; [Vulkan 단계 계획](../Release_Plan.md#v32) | 관찰: 기능 부재. 속도 우위는 미측정 | resource/shader/texture cache→3D→2D/capture→CPU 읽기→Qt 표시가 연결된 opt-in 구현 | GR-06·GR-08 oracle, GR-16 출력 계약, 출처/license 검토 | 합성 후 특성별 게임·readback·save/load·장기 사용; NVIDIA/AMD/Intel 및 공표할 OS 실제 장치 | backend 선택·명시적 제출 관리 / 큰 정확도·driver·유지보수 비용 / P3 |
| <a id="GR-16"></a>GR-16 | [GPU.h](../../src/GPU.h) Renderer::GetFramebuffers는 bool+void**로 RAM/GL을 구분하고 [Screen.cpp](../../src/frontend/qt_sdl/Screen.cpp) drawScreen은 GL texture ID로 해석. Vulkan image를 직접 꽂을 계약이 아님 | 관찰: 현재 두 표현에 묶인 인터페이스 | 두 번째 GPU API 연결 때만 frame 종류·수명·완료/반납 책임을 명시한 최소 view; 기존 SW/GL 동작 유지 | GR-11 소유권, GR-15의 2D/capture 통합 방식 선택 | 한 프레임의 생성→present→재사용·resize·교체를 backend별 검증; guest capture와 host 표시 대기를 구분 | 새 backend 연결 가능 / stale frame·조기 재사용 / P3 |
