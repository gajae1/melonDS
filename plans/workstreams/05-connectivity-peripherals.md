# 연결·디버깅·주변장치 감사와 릴리스 계획 근거

기준: 1.1.02 소스 분석. 아래는 재현·측정·구현할 후보와 검증 계약이다. 정적 의심을 확인된 결함으로 읽지 않는다. 원래 관찰은 보존하며 현재 처리 상태는 [과제 목록](../Task_Catalog.md), 실제 변경은 [릴리스 기록](../releases/1.1.03.md)을 따른다.

| ID | 현상·구체 트리거 / 파일:함수 | 수준·우선순위 | 최소 구현 단위·선행 작업 | 최소 의미 있는 검증·장치 gate | 효용·위험 |
|---|---|---|---|---|---|
| <a id="NP-01"></a>NP-01 | LAN peer가 실제보다 큰 payload 길이, AID 0/범위 밖, 범위 밖 sender를 보내는 경우. [LAN.cpp: `ProcessLAN`](../../src/net/LAN.cpp)은 최소 헤더·magic 위주 검사이고 [`RecvPacketGeneric`/`RecvReplies`](../../src/net/LAN.cpp)는 선언 길이로 복사하며 `(aid-1)*1024`와 비트 연산을 사용한다. | 정적 의심·높음 / P1 | 수신 직후 실제 길이·종류·sender·AID·연결 peer 관계를 검증하는 한 경계. 프로토콜 v1의 유효 범위부터 명문화. | 짧은 ENet payload, AID 0/상한 초과, 잘못된 sender를 넣어 무복사·무상태 변경 확인; 정상 cmd/reply/ack 왕복; 두 PC 게임 참가·교환 gate. | 범위 밖 읽기·쓰기 위험 축소; 정당한 특수 패킷까지 거부할 호환성 위험. |
| <a id="NP-02"></a>NP-02 | [LAN.cpp: `StartClient`](../../src/net/LAN.cpp)는 길이 확인 전 `data[0]`을 읽고 일부 continue/break에서 packet 해제를 건너뛴다. 5초 연결 루프를 [LANDialog.cpp](../../src/frontend/qt_sdl/LANDialog.cpp)가 직접 호출한다. `ProcessHostEvent`의 이탈 갱신은 `GetPlayerList`와 잠금 계약도 다르다. | 정적 의심·높음(길이·해제), 중간(동기화) / P1 | 먼저 packet 단일 소유·세션 초기화/종료·player 목록 동기화 정리; 다음 취소 가능한 연결 상태. NP-01, UI 작업자와 수명 계약 선행. | 빈 handshake·버전 불일치·연결 중 취소·호스트 종료·즉시 재참가에서 자원 해제와 응답성 확인; Windows/Linux 두 PC와 방화벽 차단 gate. | 실패 후 재시도 가능성; 스레드·UI 종료 순서 변경 위험. 실제 누수·race는 미재현. |
| <a id="NP-03"></a>NP-03 | 느리거나 정지한 인스턴스에 연속 송신. [LocalMP.cpp: `SendPacketGeneric`](../../src/net/LocalMP.cpp)의 FIFO full TODO와 [`RecvPacketGeneric`](../../src/net/LocalMP.cpp)의 magic 불일치 후 커서·세마포어 재설정이 남아 있다. | 관찰 + 정적 의심·중간 / P1 | 생산자·소비자 간 점유량과 넘침 정책 하나를 정의하고 기록 단위로 버리거나 역압 처리; 기존 길이 방어 유지. | 수신 정지→ring wrap→재개에서 부분 패킷 전달·무한 대기 없음; 실제 로컬 무선 게임에서 느린 인스턴스·일시정지·재개 gate. | 오염 후 복구의 예측 가능성; 대기 추가가 guest 타이밍을 바꿀 위험. |
| <a id="NP-04"></a>NP-04 | 한 프로세스에서 독립된 두 무선 그룹이 동시에 host cmd를 보냄. [LocalMP.cpp: `SendPacketGeneric`](../../src/net/LocalMP.cpp)의 단일 `MPHostinst`와 reply 상태를 공유한다. | 관찰 / P2 | 먼저 한 그룹 제한을 명확히 하거나 최소 session 식별로 분리; 임의의 다중 host 지원을 주장하지 않음. NP-03 선행. | 두 독립 host/client 쌍의 cmd/reply 교차 여부, 한쪽 종료 후 다른 쪽 유지; 실제 네 인스턴스 게임 gate. | 장시간 다중 인스턴스 신뢰성; session 분리 시 기존 LocalMP 동작 변경. |
| <a id="NP-05"></a>NP-05 | 로드 가능한 PCap 라이브러리에 필수 심볼이 없음. [Net_PCap.cpp: `LibPCap::New`](../../src/net/Net_PCap.cpp)에서 shared_ptr deleter 등록 뒤 실패 분기의 명시 unload가 중복된다. | 정적 의심·높음 / P1 | `New` 실패 경로의 라이브러리 소유자를 하나로 통일. adapter 열거 실패 자원도 같은 소유 계약으로 확인. | 심볼 누락을 주입하여 load 1회당 unload 1회, 다음 후보 로드와 정상 종료 확인; 실제 Npcap 설치/미설치 Windows 및 libpcap Linux gate. | 실패 경로 안정성; 정상 로더 선택은 보존해야 함. |
| <a id="NP-06"></a>NP-06 | `caplen < len`인 잘린 캡처. [Net_PCap.cpp: `Open`](../../src/net/Net_PCap.cpp)은 snaplen 2048, [`RXCallback`](../../src/net/Net_PCap.cpp)은 `header->len`을 callback 길이로 전달한다. [Npcap 계약][pcap]상 유효 데이터는 caplen뿐이다. | 정적 의심·높음 / P1 | 불완전 frame을 거부하고 Ethernet link type 확인; `SendPacket`/`RecvCheck`의 실제 오류와 무패킷을 구분. downstream은 완전한 Ethernet frame만 받도록 계약. | caplen 0/헤더 미만/len 미만·jumbo·비Ethernet type을 주입해 복사 범위 확인; 실제 유선/무선 어댑터·드라이버 중단 gate. | 호스트 버퍼 경계와 원인 진단; 길이만 줄여 손상 frame을 guest에 넣지 않도록 주의. |
| <a id="NP-07"></a>NP-07 | 짧은 Ethernet/IP/UDP/DNS frame, 긴/잘린 label 또는 압축 이름. [Net_Slirp.cpp: `SendPacket`](../../src/net/Net_Slirp.cpp)는 최대 길이 위주이며 고정 offset을 읽고 [`HandleDNSFrame`](../../src/net/Net_Slirp.cpp)은 이름 순회 내부의 남은 길이를 충분히 검증하지 않는다. | 정적 의심·높음 / P1 | EtherType·IPv4 IHL·UDP/DNS 길이와 label cursor를 검증; 지원하지 않는 형식의 명확한 실패 응답/폐기. 비동기 DNS는 별도 측정 후 판단. | 헤더별 truncation·옵션 있는 IPv4·잘린 label·정상 질의/오류 응답 검사; 실제 DS 네트워크 클라이언트의 DNS 성공·실패 gate. | 파서 안정성; 기존 DNS 우회 동작·이름 압축 호환성 변경 위험. |
| <a id="NP-08"></a>NP-08 | DS reply 실패·host 소실 또는 DSi 연결 매개변수 거부. [Wifi.cpp: `ProcessTX`](../../src/Wifi.cpp)의 retry 조건이 비활성이고 [`CheckRX`](../../src/Wifi.cpp)에 복구 TODO가 있다. [DSi_NWifi.cpp: `WMI_Command`/`WMI_ConnectToNetwork`](../../src/DSi_NWifi.cpp)는 일부 scan 조건 무시·실패 event 미완성이다. | 관찰 / P2 | DS는 실패/재시도/RXSTAT·TX 오류 상태표, DSi는 scan→connect→실패→disconnect event부터 좁게 완성. WEP·power 세부는 실기 근거 확보 후. CPU timer/IRQ 계약 인계. | host 소실·잘못된 auth/channel·scan 취소의 guest event/IRQ 추적; DS/DSi 게임과 실제 장치의 같은 시퀀스 비교. 과거 BIOS 현상은 별도 원본 비교. | 게임이 연결 실패에서 복구하도록 개선; 근거 없이 retry를 켜면 동기화가 악화될 수 있음. |
| <a id="NP-09"></a>NP-09 | [Netplay.cpp: `DeInit`, `SpawnMirrorInstance`, blob 동기화](../../src/net/Netplay.cpp)에 cleanup TODO와 비활성 경로가 남고 제품 연결이 없다. | 관찰 / P2·실험 | 지원 topology·동기화 대상·종료 계약을 먼저 선택, 최소 host/client 게임 한 흐름만 연결. R1/R2, CPU 결정성·상태 담당의 계약 선행. | 연결·동일 상태 확인·입력·실제 게임 완료·이탈·종료를 연속 수행; 서로 다른 PC/OS, 손실·지연·clock 차이 gate. 합성 왕복만으로 승격 금지. | 원격 플레이 사용 가능성; mirror 구조·대역폭·state 호환성·실제 지연은 미측정. |
| <a id="NP-10"></a>NP-10 | 수신 버퍼 1152바이트를 꽉 채우면 [GdbProto.cpp: `TryParsePacket`](../../src/debug/GdbProto.cpp)이 끝에 NUL을 기록한다. [`Resp`](../../src/debug/GdbProto.cpp)는 payload만 capacity와 비교해 framing+NUL 공간을 남기지 않는다. `Handle_m` 한도와 광고 PacketSize도 불일치한다. | 정적 의심·높음 / P1 | payload/encoded frame/terminator 용량 계약 하나로 송수신·`qSupported`·`m` 한도를 맞춤. [GDB framing][gdb-overview] 기준. | 한도 직전/정확히 한도/초과, 분할·병합 수신, 최대 정상 `m` 응답의 guard와 응답 완료 확인; 실제 GDB ARM7/9 연결 gate. | 버퍼 경계와 큰 메모리 조회 안정성; PacketSize 협상 호환성 주의. |
| <a id="NP-11"></a>NP-11 | `M/X`의 colon 누락·선언 길이보다 짧은 body·주소 wrap. [GdbCmds.cpp: `Handle_M`](../../src/debug/GdbCmds.cpp)는 `%n`까지 처리됐는지와 body 길이를 확인하지 않는다. [`Handle_X`](../../src/debug/GdbCmds.cpp)의 마지막 1바이트가 `unhex8`이며 binary escape 해제도 경로에서 확인되지 않는다. | 정적 의심·높음 / P1 | 길이를 가진 parser로 hex/binary 구분, escape 해제 뒤 정확한 byte 수 확인, 잘못된 명령은 guest 쓰기 전 거부. CPU memory callback 폭·MMIO 의미는 해당 담당과 유지. | 4바이트 정렬 주소에 5바이트 `X`, `$ # }`/NUL 포함, 짧은 `M`, colon 누락·wrap에서 결과와 무부분쓰기 확인; [공식 X 계약][gdb-packets]과 실제 GDB `load`/메모리 수정 gate. | 잘못된 guest 메모리 쓰기 방지; 정당한 MMIO 접근 폭을 임의 변경할 위험. |
| <a id="NP-12"></a>NP-12 | no-ack 협상/부분 패킷 뒤 재접속. [GdbStub.cpp: `Disconnect`](../../src/debug/GdbStub.cpp)는 fd만 닫고 no-ack·수신 잔량을 초기화하지 않는다. [GdbProto.cpp: `MsgRecv`/`Resp`](../../src/debug/GdbProto.cpp)는 EOF/부분 송신 계약이 약하며 Windows `Poll`의 음수 timeval·select 오류 처리도 의심된다. | 정적 의심·높음(재접속), 중간(OS·I/O) / P1 | 연결 상태 초기화, EOF·EAGAIN·부분 송신 구분, 취소 가능한 대기; OS별 socket 형·invalid sentinel 정리. NP-10, [Winsock select 계약][winsock-select] 선행. | no-ack→절단→새 클라이언트, 반 패킷 후 종료, send 분할·port 점유, pause 중 detach/종료; Windows/Linux 실제 GDB 두 포트 gate. | 반복 디버깅·종료 안정성; blocking 변경과 emulation pause의 결합 위험. |
| <a id="NP-13"></a>NP-13 | 1.1.10에서 [CartRetailIR.cpp](../../src/NDSCart/CartRetailIR.cpp)의 불확정 응답과 IRPos Reset/복원 누락을 수정했다. | 실행 재현 / P1 | FF fallback·포화 위치·14.1 직렬화·자체 section 경계, 14.0 새 명령 시작. [구현 증거](../releases/1.1.10.md) | 생산 카트 ID/SRAM·중간 복원·잘린 상태 11개 통과; 실제 IR 송수신과 물리 응답값 후속. | UB 제거·복원 일관성; 호환성 fallback을 실기 정확성으로 포장하지 않는다. |
| <a id="NP-14"></a>NP-14 | UZPx 카트리지는 BT 클래스로 선택되지만 [CartRetailBT.cpp: `SPITransmitReceive`](../../src/NDSCart/CartRetailBT.cpp)는 항상 0을 반환한다. | 관찰 / P2·실험 | Pokémon Typing Adventure의 SPI 초기화·keyboard 상태·해제 흐름 하나부터 구현. OS Bluetooth 전체 stack을 만들기 전에 가상 키보드 계약과 실제 카드 응답 확보. | command trace 재생과 실제 게임의 초기화→입력→키 해제→재연결, 실물 카드/키보드 기준 gate. | 특정 주변장치의 실제 사용 가능성; protocol 미확인·입력 지역/배열 차이. |
| <a id="NP-15"></a>NP-15 | [NDSCart.cpp: WriteSPICnt](../../src/NDSCart.cpp)의 하위 byte 쓰기가 보존된 bit13을 잘못된 CS 해제로 전달했다. 전송 중 CPUSelect·전원·속도·DRQ 타이밍 TODO도 남는다. | 부분 실행 재현 / P2 | [1.1.11](../releases/1.1.11.md)에서 mask 뒤 실제 mode 전이로 수정. 소유·busy·정상 mode·eject를 대조하며 기존 IRQ 유지. | ARM9/ARM7 MMIO·scheduler·카트·전체 상태 복원 회귀; 실제 전송 중 속도/소유/전원 event 순서·ROM DMA 병행 후속. | held WRITE 데이터 보존; 일부 CS 수정을 전체 타이밍 정확성으로 확대하지 않는다. |
| <a id="NP-16"></a>NP-16 | [CartHomebrew.cpp: `SetupDirectBoot`](../../src/NDSCart/CartHomebrew.cpp)는 512바이트 argv에 `fat:/` 뒤 최대 511바이트를 붙인다. SD 주입 성공과 ROM 이름 506바이트 초과가 조건이며 UI 도달성은 미확인. [CartSD.cpp: `ApplyDLDIPatch`/`ApplyDLDIPatchAt`](../../src/NDSCart/CartSD.cpp)는 tail signature 이후 header/patch 범위를 별도로 보장하지 않는다. | 정적 의심·중간(도달성 미확인) / P1 | argv의 남은 용량·종결을 계산; DLDI 함수에 실제 binary span을 전달해 signature/header/복사 범위 검증. 이미 고친 NDS header 검사를 대체하지 않음. | 긴 UTF-8 이름·마지막 부근 DLDI signature·할당보다 큰 patch와 정상 homebrew 비교; 실제 DLDI SD 읽기/쓰기·읽기 전용 gate. | homebrew 입력 경계; patch를 조용히 잘라 정상 driver를 손상시키지 않도록 주의. |
| <a id="NP-17"></a>NP-17 | [GBACart.cpp: `LoadAddon`, `CartGameSolarSensor::ProcessGPIO`, `CartRumblePak::ROMWrite`](../../src/GBACart.cpp)의 기존 장치와 [GBACartMotionPak.cpp: `SRAMRead`](../../src/GBACartMotionPak.cpp)의 축 부호·직렬화 CHECKME를 장치별로 대조해야 한다. | 관찰 / P2 | RAM lock/용량, 센서 범위·sample phase, 진동 시작/정지의 계약을 분리하고 확인된 불일치만 수정. frontend 입력 담당은 callback hotplug 계약 담당. | 태양광 양끝·모션 고정 자세/회전·Guitar 동시 키·RAM lock, pause/eject/장치 교체 시 진동 종료; 센서·진동 지원/미지원 실제 pad와 대상 게임 gate. | 이미 있는 주변장치의 신뢰성; 미측정 축 변환을 정답으로 가정할 위험. |
| <a id="NP-18"></a>NP-18 | [CartRetail.cpp](../../src/NDSCart/CartRetail.cpp)의 tiny EEPROM·GBA bus/식별·DS 용량/여분·Flash에 이어1.1.75 명시적 EEPROM/FRAM 매체와 페이지/연속 쓰기를 구현. | 부분 실행 재현 / P2 | 기존 자동 판별/코드2~4는 유지하고 명시EEPROM11~13·FRAM14를 추가한다. 같은 용량도 매체를 복원하며 명시적 매체만14.7, 기존 일반14.2·Flash 대기14.6을 유지한다. 파일 여분 보존. | 수정 전 칩3/소비자4실패→관련41통과, JIT OFF26통과. DS/DSi/IR 전송5경로·prime Flush 후 파일 보존·손상 매체/latch 거부·실제1.1.73 writer/reader8회 대조. | 검증된 자동 식별 정보 확장, NAND 제조사 영역·WIP/WP·칩 변종/timing·실기/native ARM·전체 복원 실패 원자성 후속. |
| <a id="NP-19"></a>NP-19 | [RTC.cpp: `SetDateTime`, `WriteDateTime`, `ProcessIRQ`, `ClockTimer`](../../src/RTC.cpp)와 [EmuInstance.cpp: `syncRTC`](../../src/frontend/qt_sdl/EmuInstance.cpp)가 host/guest 시간 정책 경계를 이룬다. host 시간 점프·동기화 전환·12시간 정오/자정이 구체 점검 조건이다. | 관찰·미측정 아이디어 / P2 | 기존 offset/SyncToHost와 guest 진행의 우선순위 문서화, 실패가 재현된 달력/IRQ 전이만 수정. 32768Hz event 축약은 별도 측정 전 보류. | 2/28~3/1·윤년·2099→00·12/24시간·잘못된 BCD·alarm IRQ pulse, pause/배속/host DST 변경·복원; 날짜 의존 게임과 DS/DSi 실기 gate. | 시간 기반 이벤트 일관성; 모든 host 보정을 버그로 단정하거나 IRQ를 초 단위로 뭉개면 회귀. |
| <a id="NP-20"></a>NP-20 | 잘린 usrcheat DB header/entry/body. [ARDatabaseDAT.cpp: `LoadEntries`/`LoadCheatCodes`](../../src/ARDatabaseDAT.cpp)는 일부 FileRead 결과를 확인하지 않고, [`GetEntriesByGameCode`](../../src/ARDatabaseDAT.cpp)는 LoadCheatCodes 실패를 무시한다. raw code short read가 미초기화 word로 이어질 정적 위험이다. | 정적 의심·높음 / P1 | 파일 길이·read/seek 성공·문자열 끝·word 수를 확인하고 성공한 entry만 반환; 오류 위치를 기존 import 흐름에 전달. `.mch`의 기존 오류 차단 유지. | truncation 지점을 header/entry/string/code로 제한해 부정확한 import 없음 확인; 정상 gamecode/checksum·단일 활성화·실제 DB 선택 흐름 gate. | 손상 입력의 실행 방지; 부분 정상 entry를 허용할지 정책을 명확히 해야 함. |
| <a id="NP-21"></a>NP-21 | 기존 [AREngine.cpp: `RunCheat`](../../src/AREngine.cpp)의 빈/홀수/literal 경계와 긴 실행 취소를 1.1.09에서 처리. 1.1.13에는 NEXT의 조건 복원 누락과 이전 조건 없는 D0의 후속 쓰기 억제를 재현. | 실행 재현 / P1 | D1/D2 매 반복에서 C0 조건 복원, D0 기본 실행 상태, D2 최종 레지스터 초기화. 기존 입력 사전 검사와 stop_token 취소·실행본 비활성화 유지. C4·C5 수명은 원 핸들러 증거 없이 추측 구현하지 않음. | 실제 ARM7 IRQ·생성 메모리의 false/true/false·0/2 count·단일 loop 교체·취소 회귀, C5 수명과 무관한 mask 대조. Datel 전체 명령·실제 게임 치트 적용/해제·복원 gate 후속. | host 접근·멈춤·조건부 반복 정확성; 임의의 작은 실행 상한과 근거 없는 영속 counter를 도입하지 않음. |
